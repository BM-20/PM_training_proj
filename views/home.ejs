<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>RedTrade - Home</title>
  <style>
    :root {
      --bg:#000000;
      --panel:#0b0b0b;
      --panel-2:#101010;
      --text:#f5f5f5;
      --muted:#b3b3b3;
      --red:#e11d48; /* red-600 vibe */
      --red-700:#be123c;
      --ring:rgba(225,29,72,.35);
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }
    html, body { height: 100%; }
    body {
      font: 16px/1.45 system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,Cantarell,"Helvetica Neue",Arial,"Noto Sans",sans-serif;
      background: var(--bg);
      color: var(--text);
    }

    /* Splash layout */
    .splash {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      background: radial-gradient(circle at top left, #111, #000);
      text-align: center;
      padding: 2rem;
    }

    /* Hero */
    .splash-hero {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 1.2rem;
      max-width: 800px;
      margin: auto;
      animation: fadeUp 0.8s ease-out both;
    }
    .splash-hero h1 {
      font-size: clamp(2.2rem, 6vw, 3.5rem);
      font-weight: 700;
    }
    .splash-hero h1 span {
      color: var(--red);
    }
    .splash-hero p {
      font-size: 1.15rem;
      color: var(--muted);
      line-height: 1.6;
      max-width: 600px;
      margin: 0 auto;
    }

    /* CTA Buttons */
    .splash-actions {
      margin-top: 1.5rem;
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }
    .btn {
      border: 1px solid #2a2a2a;
      padding: 12px 20px;
      font-size: 1.05rem;
      border-radius: 12px;
      cursor: pointer;
      font-weight: 600;
      transition: transform .06s ease, background .2s ease, border-color .2s ease;
      text-decoration: none;
      display: inline-block;
    }
    .btn:active { transform: translateY(1px); }
    .btn-primary {
      background: var(--red);
      border-color: var(--red);
      color: white;
    }
    .btn-primary:hover {
      background: var(--red-700);
      border-color: var(--red-700);
      transform: translateY(-2px);
    }
    .btn-ghost {
      background: transparent;
      border-color: #333;
      color: var(--text);
    }
    .btn-ghost:hover {
      border-color: var(--red);
      color: var(--red);
    }

    /* Features */
    .splash-features {
      display: grid;
      gap: 1.5rem;
      margin-top: 3rem;
      animation: fadeUp 1s ease-out both;
    }
    @media (min-width: 768px) {
      .splash-features {
        grid-template-columns: repeat(3, 1fr);
      }
    }
    .feature-card {
      background: var(--panel);
      border: 1px solid #1a1a1a;
      border-radius: 12px;
      padding: 1.5rem;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    .feature-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 0 15px rgba(225,29,72,.4);
    }
    .feature-card h3 {
      color: var(--red);
      margin-bottom: 0.5rem;
    }
    .feature-card p {
      color: var(--muted);
      font-size: 0.95rem;
    }

    /* Footer */
    .splash-footer {
      padding: 1rem 0;
      font-size: 0.85rem;
      color: var(--muted);
      border-top: 1px solid #1a1a1a;
    }

    /* Animations */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <div class="splash">
    <section class="splash-hero">
      <h1>Welcome to <span>RedTrade</span></h1>
      <p>Track your portfolio, analyze markets, and chat with AI — all in one sleek dashboard.</p>
      <div class="splash-actions">
        <a href="auth/register" class="btn btn-primary">Get Started</a>
        <a href="auth/login" class="btn btn-ghost">Login</a>
      </div>
    </section>

    <section class="splash-features">
      <div class="feature-card">
        <h3>📊 Real-Time Tracking</h3>
        <p>Stay updated with live stock data and portfolio performance.</p>
      </div>
      <div class="feature-card">
        <h3>🤖 AI Insights</h3>
        <p>Smart recommendations and market insights tailored for you.</p>
      </div>
      <div class="feature-card">
        <h3>💬 Community Chat</h3>
        <p>Discuss trends and strategies with fellow traders.</p>
      </div>
    </section>

    <footer class="splash-footer">
      &copy; 2025 RedTrade. All rights reserved.
    </footer>
  </div>
</body>
</html>
